import logo from '../../../public/images/logo.png';
const Logo = () => {
    return (
        <div>
            <img className='w-[220px]' src={logo} alt="" />
        </div>
    );
};

export default Logo;